apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ .Release.Name }}-standalone"
  name: "{{ .Release.Name }}-service"
spec:
  ports:
  - port: 27017
    protocol: TCP
    targetPort: 27017
    nodePort: 32000
  selector:
    app: "{{ .Release.Name }}-standalone"
  type: NodePort
  